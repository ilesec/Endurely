# ========================================
# LLM Provider Configuration
# ========================================
LLM_PROVIDER=azure_ai

# ========================================
# Azure OpenAI Settings
# ========================================
# Azure OpenAI endpoint URL (without /openai/v1 suffix)
AZURE_AI_ENDPOINT=https://your-endpoint.openai.azure.com/

# Deployment/model name (e.g., gpt-4o-mini, gpt-4o)
AZURE_AI_DEPLOYMENT_NAME=gpt-4o-mini

# Authentication: "entra_id" (Managed Identity) or "api_key"
# Managed identity is recommended for Azure deployment (no keys needed!)
AZURE_AI_AUTH=entra_id

# API Key (only needed if AZURE_AI_AUTH=api_key)
# AZURE_AI_API_KEY=your_azure_ai_key_here

# Optional: User-assigned managed identity client ID
# Leave empty to use system-assigned managed identity (default)
# AZURE_AI_MANAGED_IDENTITY_CLIENT_ID=

# ========================================
# Authentication (Entra External ID)
# ========================================
# Set to false for development without authentication
ENABLE_AUTH=false

# Microsoft Entra External ID configuration (required if ENABLE_AUTH=true)
# ENTRA_TENANT_ID=your-external-id-tenant-id
# ENTRA_CLIENT_ID=your-client-id
# ENTRA_CLIENT_SECRET=your-client-secret
# ENTRA_REDIRECT_URI=http://localhost:8000/auth/callback
# ENTRA_CIAM_DOMAIN=your-domain (e.g., "endurely" for endurely.ciamlogin.com)
# SESSION_SECRET_KEY=generate-with-openssl-rand-hex-32

# ========================================
# Database Configuration
# ========================================
# Local development (SQLite)
DATABASE_URL=sqlite:///./workouts.db

# Production (PostgreSQL example)
# DATABASE_URL=postgresql://user:password@localhost:5432/endurely_db
