# ========================================
# LLM Provider Configuration
# ========================================
# Azure OpenAI only
LLM_PROVIDER=azure_ai

# ========================================
# Azure OpenAI Settings
# ========================================
# Azure AI Studio endpoint URL
AZURE_AI_ENDPOINT=https://your-endpoint.openai.azure.com/

# Authentication method: "api_key" or "entra_id" (Managed Identity)
AZURE_AI_AUTH=entra_id

# API Key (only needed if AZURE_AI_AUTH=api_key)
# AZURE_AI_API_KEY=your_azure_ai_key_here

# Deployment/model name (e.g., gpt-4o-mini, gpt-35-turbo)
AZURE_AI_DEPLOYMENT_NAME=gpt-4o-mini

# Azure OpenAI API version
AZURE_AI_API_VERSION=2024-02-15-preview

# Optional: User-assigned managed identity client ID (for Entra ID auth)
# Leave empty for system-assigned managed identity
# AZURE_AI_MANAGED_IDENTITY_CLIENT_ID=

# ========================================
# Authentication (Entra External ID)
# ========================================
# Set to false for development without authentication
ENABLE_AUTH=false

# Microsoft Entra External ID configuration
# ENTRA_TENANT_ID=your-external-id-tenant-id
# ENTRA_CLIENT_ID=your-client-id
# ENTRA_CLIENT_SECRET=your-client-secret
# ENTRA_REDIRECT_URI=http://localhost:8000/auth/callback
# ENTRA_CIAM_DOMAIN=your-domain (e.g., "triathlonapp" for triathlonapp.ciamlogin.com)
# SESSION_SECRET_KEY=generate-with-python-secrets

# ========================================
# Database Configuration
# ========================================
# Local development (SQLite)
DATABASE_URL=sqlite:///./workouts.db

# Production (PostgreSQL example)
# DATABASE_URL=postgresql://user:password@localhost:5432/triathlon_db

# Docker (use volume mount)
# DATABASE_URL=sqlite:///./data/workouts.db
